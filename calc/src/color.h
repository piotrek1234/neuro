#ifndef COLOR_H
#define COLOR_H

/**
 * @brief The Color enum is used to distinguish players and their tokens
 */
enum class Color{
    BLUE = 1,
    RED = 2,
    YELLOW = 3,
    GREEN = 4,
    NONE = 0,
};

#endif // COLOR_H

